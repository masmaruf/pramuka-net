import { Award, Pencil, Flame, Heart, Bird, Compass } from "lucide-react";
import type { ElementType } from "react";

export const badgeIconMap: Record<string, ElementType> = {
  pencil: Pencil,
  flame: Flame,
  award: Award,
  heart: Heart,
  eagle: Bird,
  compass: Compass,
};
